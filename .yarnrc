# Yarn v1 reads .yarnrc from cwd where `yarn install` runs (repo root on Vercel).
# Skip optional platform-specific deps that can't install on linux/x64
# e.g. @rolldown/binding-openharmony-arm64 (os: openharmony, cpu: arm64).
ignore-optional true
ignore-engines true
